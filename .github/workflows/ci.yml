name: CI

on:
  push:
    branches: ['**']
    tags-ignore: ['v*']   # releases are handled by release.yml
  pull_request:

permissions:
  contents: read

jobs:
  build:
    runs-on: windows-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up MSVC x64 environment
        uses: ilammy/msvc-dev-cmd@v1
        with:
          arch: x64

      - name: Build zlib from source
        shell: cmd
        run: |
          curl -fsSL https://zlib.net/zlib-1.3.1.tar.gz -o zlib.tar.gz
          tar -xf zlib.tar.gz
          cd zlib-1.3.1
          nmake /f win32\Makefile.msc
          cd ..
          if not exist dist\zlib\lib\x64 mkdir dist\zlib\lib\x64
          copy zlib-1.3.1\zlib.lib dist\zlib\lib\x64\zlib.lib

      - name: Build
        shell: cmd
        run: nmake build
